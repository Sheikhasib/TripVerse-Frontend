// Placeholder — replaced with the real home page (hero + sections) in Phase C
// of Step 6. The route group layout (navbar + footer) renders around it.
export default function HomePage() {
  return (
    <main className="flex min-h-[60vh] flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-2xl font-semibold">TripVerse</h1>
      <p className="text-muted-foreground">
        Public shell is up — home sections land next.
      </p>
    </main>
  )
}