// Placeholder — replaced with the real home page (hero + 8 sections) in
// Step 6 — Public Pages. This exists only so `npm run dev` renders
// something at "/" while Steps 1-2 are being verified.
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-2xl font-semibold">TripVerse</h1>
      <p className="text-muted-foreground">
        Project setup + app shell done (Steps 1–2). Real pages come in Step 6.
      </p>
    </main>
  )
}
